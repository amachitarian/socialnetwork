<div class="container">
  <h1><%= @user.id %></h1>
  <h2><%= @user.name %></h2>
  <p><%= @user.followers.count %></p>
  <p><%= link_to "Follow", follow_user_path(@user), class: "btn btn-info follow", data: {user_id: @user.id}  %></p>
  <p><%= link_to @user.followers.count,"" ,id: "follow-count-" + @user.id.to_s , class: "label label-info"%></p>
</div>
