<div class="container">
<%= @user.name %>

<%= form_for(current_user.me_following.build) do |f| %>

  <div><%= f.hidden_field :followed_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>

</div>
